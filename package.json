{
  "name": "erp-aboca",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm install --prefix backend --ignore-scripts && npm --prefix frontend install --include=dev && npm --prefix frontend run build",
    "start": "node backend/server.js",
    "lint:backend": "cd backend && npm run lint",
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.0.0",
    "lint-staged": "^15.0.0"
  }
}
